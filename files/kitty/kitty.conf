cursor_trail                1
window_padding_width        25
confirm_os_window_close     0

# Start or reattach Herdr for plain terminal launches. Explicit commands passed
# to kitty (for example, `kitty -e btop`) continue to run normally.
shell                       /usr/bin/herdr

include themes/noctalia.conf
